#!/usr/bin/env bash
# Print installable Android API levels, space-separated.
# Descending (newest first): avm's "latest"/"recent" take the leading tokens.
# ponytail: curated static list; derive from `sdkmanager --list` if it drifts.
set -euo pipefail
echo "35 34 33 32 31 30"
